#include "bucksort.hpp"

int main(int argc, char *argv[])
{
    ListNode *head=new ListNode();
    ListNode *p = new ListNode(45);

    ListNode *s=insert(head, 3);
    s = new ListNode(5);
    return 0;
}